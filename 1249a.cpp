#include<bits/stdc++.h>

using namespace std;

int main(){

    int t,size;
    cin >> t;
    while(t--){
        vector<int> v1;
        cin >> size;
        while(size--){
            int t;
            cin >> t;
            v1.push_back(t);
        }
    }
    
}